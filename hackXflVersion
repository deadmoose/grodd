#!/usr/bin/env perl
#
# https://github.com/deadmoose/grodd
#
# The xflVersion keeps getting bumped even though (in my experience, at least), the
# format hasn't been changing, and I'm capable of reading files generated by future
# versions just fine if I hack them to claim they're older.
#
# I wouldn't recommend using this for production work, but it's certainly good enough
# for nosing around in files. Specifically hacks to claim to be version 2.0 (aka CS5)
# because that's all I really care about

local $^I = '.bak';
local @ARGV = ("DOMDocument.xml");

while (<>) {
    s/xflVersion=".+?"/xflVersion="2.0"/;
    print;
}
